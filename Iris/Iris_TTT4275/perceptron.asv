function perceptron(trainingSet)
    learningRate = 0.01;
    maxIter = 10000;
    weights = zeros(1, 2);
    iter = 0;
    while iter < maxIter
        randIndex = randi(size(trainingSet, 1));
        if trainingSet(randIndex, 
    end
    

end