load('data_all.mat');

num_clusters = 64;
dimensions = 28*28;
clusters = zeros(num_clusters*10,dimensions);
cluster_labels = zeros(num_clusters*10,1);
for i = 1:10
    class_indeces = find(trainlab == i-1);
    class_vectors = trainv(class_indeces,:);
    [idx,C] = kmeans(class_vectors,num_clusters);
    clusters(((i-1)*num_clusters):(i*num_clusters),:) = C;
    cluster_labels((i
end
